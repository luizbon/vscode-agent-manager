---
trigger: always_on
---

This code runs on all platforms, including Mac/Linux/Windows, and is especially useful for path solutions.

For all new features, make sure the correct telemetry is in place. The only way to capture and improve the code is through telemetry feedback, so this is most effective when writing code.

All features should be covered by tests.